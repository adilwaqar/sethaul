# For local development (Vite proxy handles /api → localhost:8000)
VITE_API_URL=/api

# For production (Vercel) — point to your Railway/Render backend URL
# VITE_API_URL=https://your-backend.railway.app
